autoscale is used to horizontally scale the pods when required 
for example :
kubectl autoscale deployments deployment-name --cpu=50% --min=1 --max=10 
will autoscale the number of pods when cpu usage hits 50% and will have the limit to create between 2 to 10 pods
